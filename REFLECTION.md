A GET HTTP Request gets sent from the browser to a server that stores
the files of our website to retrieve the index.html file in our GitHub repo.
Then, the server locates the index.html file on its disk, and if it's there,
it will send an HTTP Response back to the browser. The HTTP Response will
contain the index.html file and the browser will render the index.html file.